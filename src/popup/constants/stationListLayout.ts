/**
 * Minimum row height for {@link StationCard} in browse/favourites Virtuoso lists.
 * Keep in sync: `StationCard` `minHeight`, `StationList` / `FavouritesStationList`
 * `defaultItemHeight`, loading skeleton bars, and optional preview rows in add/edit modals.
 */
export const STATION_LIST_ROW_HEIGHT_PX = 60;
